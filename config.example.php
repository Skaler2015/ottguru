<?php
/**
 * ============================================================================
 *  OTT Guru — सेटिंग का नमूना
 *  इसकी नक़ल बनाकर config.php नाम दीजिए और अपनी जानकारी भरिए।
 *  config.php git में नहीं जाती (.gitignore में है)
 *  सिर्फ़ यही फाइल बदलनी है। बाक़ी कोड को छूने की ज़रूरत नहीं।
 * ============================================================================
 */

return [

    // ---------------------------------------------------------------- डेटाबेस
    // hPanel → Databases → MySQL से मिलेगा
    'db' => [
        'host'     => 'localhost',
        'name'     => 'uXXXXXX_ottguru',
        'user'     => 'uXXXXXX_ott',
        'pass'     => 'यहाँ-पासवर्ड',
        'port'     => 3306,
        // Hostinger पर आम तौर पर socket की ज़रूरत नहीं। स्थानीय टेस्ट के लिए ही।
        'socket'   => null,
    ],

    // ---------------------------------------------------------------- TMDB
    // v3 API key या v4 Read Access Token — दोनों चलेंगे
    'tmdb_key' => 'यहाँ-TMDB-key',

    // TMDB के दो आधिकारिक पते हैं। भारत के सर्वरों (Hostinger Mumbai समेत) से
    // api.themoviedb.org पर रुक-रुककर connection reset आता है — असली deployment
    // में परखा गया (5 में से 2-3 कॉल टूटती थीं)। api.tmdb.org साफ़ चला, इसलिए
    // वही default है। दिक़्क़त हो तो जाँच: README का "TMDB तक पहुँच" हिस्सा।
    'tmdb_base' => 'https://api.tmdb.org/3',

    // ------------------------------------ Streaming Availability (deep links + dub ऑडियो)
    // RapidAPI का "Streaming Availability" (movieofthenight)। यही असली per-OTT
    // deep link (सीधे उस मूवी पर) और "इस OTT पर कौन सी ऑडियो/dub" देता है।
    // पहले RapidAPI पर subscribe कीजिए (मुफ़्त tier भी है), फिर X-RapidAPI-Key यहाँ डालिए।
    // 'key' खाली = फ़ीचर बंद (कुछ नहीं टूटता, TMDB वाला सब चलता रहता है)।
    'sa' => [
        'key'     => '',   // RapidAPI key
        'host'    => 'streaming-availability.p.rapidapi.com',
        'per_run' => 40,   // हर दौड़ में कितने titles (rate-limit के हिसाब से)
    ],

    // ---------------------------------------------------------------- सुरक्षा
    // ब्राउज़र से स्क्रिप्ट चलाने के लिए ज़रूरी: ?k=यह-टोकन
    // cron (CLI) से चलाने पर टोकन नहीं चाहिए।
    // इसे लंबा और अटपटा रखिए। खाली छोड़ना = कोई भी आपका sync चला सकता है।
    'run_token' => 'बदल-कर-कुछ-लंबा-लिखिए',

    // गुप्त admin path — इसे सेट करते ही admin dashboard सीधे /<यह-value> पर खुलता है।
    // जैसे 'skaler2015' → https://ottguru.in/skaler2015 । /admin भी चलता है।
    // यह एक अतिरिक्त परत है (URL पता होना) — किसी को न बताइए, 'admin' जैसा आसान न रखिए।
    // खाली छोड़ें तो admin सिर्फ़ /admin पर खुलेगा।
    'admin_path' => '',

    // admin login का पासवर्ड — /admin (या गुप्त path) खोलने पर यही माँगा जाता है।
    // एक बार login → session में याद रहता है, "लॉगआउट" से बाहर।
    // खाली छोड़ें तो run_token ही पासवर्ड बन जाता है (अलग से कुछ सेट किए बिना चल जाए)।
    'admin_pass' => '',

    // ---------------------------------------------------------------- क्षेत्र
    'country'  => 'IN',
    'language' => 'en-US',   // TMDB मेटाडेटा की भाषा

    // ---------------------------------------------------------------- रफ़्तार
    // Hostinger के max_execution_time से बचने के लिए हर दौड़ छोटी रखिए।
    // बढ़ाने से पहले status.php पर 'अवधि' देख लीजिए।
    'batch' => [
        'catalog_pages_per_run'  => 8,    // discovery में कितने पेज
        'provider_titles_per_run'=> 250,  // providers में कितने titles
        'max_seconds'            => 240,  // इससे ज़्यादा चला तो अपने-आप रुककर कर्सर सहेजेगा
        'sleep_ms'               => 120,  // हर API कॉल के बीच ठहराव
    ],

    // ---------------------------------------------------------------- कब-कब जाँचें
    // tier 1 = चर्चित/नया, 2 = मध्यम, 3 = पुरानी long tail
    'poll_hours' => [
        1 => 20,    // रोज़
        2 => 144,   // ~हफ़्ते में
        3 => 648,   // ~महीने में
    ],

    // ---------------------------------------------------------------- सुरक्षा जाँच
    // एक बुरा sync पूरी history बर्बाद कर सकता है। ये उसका ब्रेक है।
    // अगर एक ही दौड़ में इतने % से ज़्यादा titles से provider "हटते" दिखें,
    // तो कुछ भी लिखा नहीं जाएगा — दौड़ halted हो जाएगी और मेल आएगा।
    'safety' => [
        'max_removal_pct' => 40,   // प्रतिशत
        'min_batch'       => 20,   // इससे छोटी दौड़ पर जाँच नहीं लगेगी
        'alert_email'     => '',   // खाली = मेल नहीं
    ],

    // ---------------------------------------------------------------- API
    'http' => [
        'timeout'     => 25,
        'retries'     => 2,     // पहली कोशिश के बाद कितनी बार और
        'retry_sleep' => 1200,  // मिलीसेकंड
    ],

    // ---------------------------------------------------------------- page-cache
    // हल्का file-based full-page cache (Redis का shared-hosting विकल्प)।
    // डेटा सिर्फ़ रात की sync में बदलता है, इसलिए दिन भर पेज static रहते हैं —
    // HIT पर पूरी DB-query + rendering बच जाती है। sync पूरा होने पर अपने-आप साफ़।
    // admin/search/sitemap कभी cache नहीं होते। बंद करना हो तो 'enabled' => false.
    // 'dir' public_html के बाहर होना चाहिए (config.php की तरह)।
    'cache' => [
        'enabled' => true,
        'ttl'     => 7200,               // सेकंड — sync छूट भी जाए तो इतने बाद ताज़ा
        'dir'     => __DIR__ . '/cache', // बदल-कर public_html के बाहर कोई फोल्डर
    ],

    // ---------------------------------------------------------------- लॉग
    'log_dir' => __DIR__ . '/logs',
];
